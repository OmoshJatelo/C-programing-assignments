#include <stdio.h>
#include <stdlib.h>
#include <mathlib.h>

int quantity;
float total_price, credit_limit;
int main(){

do{

    printf("Enter the quantity:\n");
    scanf(%f d,&quantity);
    total_price = price * quantity;

    if(total_price > credit_limit){
        printf("You cannot purchase goods worth such avalue on credit.\n");
    }
}while(total_price > credit_limit){
    printf("Thank you for purchasing from us\nthe total price is: %d\n", total_price);
    counter ++;

}
    return 0;
}
